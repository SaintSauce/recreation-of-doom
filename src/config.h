#ifndef CONFIG_H
#define CONFIG_H

#define RES 5
#define WIN_WIDTH (160 * RES)
#define WIN_HEIGHT (120 * RES)
#define HALF_WIN_WIDTH (WIN_WIDTH/2)
#define HALF_WIN_HEIGHT (WIN_HEIGHT/2)

#define PIXEL_SCALE 4/RES // OpenGL pixel scale

#endif